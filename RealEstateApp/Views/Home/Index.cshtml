
@{
    // var model = Model.Houses.Select(h => h);
    
    ViewData["Title"] = "Home Page";
}

@* <div class="row">
    @foreach (var house in model)
    {
        <div class="col-md-4 mb-4">
           
                    <h5 class="card-title text-primary">@house.Title</h5>
                    @* <p class="card-text text-muted">
                        <i class="bi bi-geo-alt"></i> @house.Location
                    </p> *@
                    @* <div class="d-flex justify-content-between align-items-center">
                        <span class="badge bg-success fs-6">@house.Price.ToString("C0")</span>
                        <small class="text-secondary">@house.NumberOfRooms Odalı</small>
                    </div>
                </div>
                <div class="card-footer bg-white border-top-0">
                    <a href="/House/Details/@house.Id" class="btn btn-outline-primary w-100">Detayları Gör</a>
    }
</div> 
                </div>          *@
